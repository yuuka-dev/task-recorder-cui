# インタラクティブメニュー (Phase 6) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `tsk` をサブコマンドなしで起動した時に questionary ベースの階層メニューを表示し、記録系・参照系・カテゴリ管理・ヘルプに到達できるようにする。

**Architecture:** `menu.py` を新規追加し、`cli.py` の `args.command is None` 分岐から呼び出す。pure 関数 (ステータス行、直近一覧整形、相対時刻整形) と UI 層 (questionary を使うループ) を分離。pure 関数は単体テスト、UI は最小スモークテスト 1 本で抑える。menu.run() は常に 0 を返し、内部の commands.*.run() の戻り値は意図的に捨てる。

**Tech Stack:** Python 3.11+, `questionary`, 既存 `rich`, `pytest`, `pytest-cov`, 既存 commands 層 / repo 層をそのまま再利用。

**関連ドキュメント:**
- 設計書: `docs/superpowers/specs/2026-04-15-menu-design.md`
- Issue: #13
- プロジェクト仕様: `CLAUDE.md` (Phase 6 / メニュー構造モックアップ)

---

## File Structure

| パス | 種別 | 責務 |
|---|---|---|
| `src/task_recorder_cui/utils/time.py` | Modify | `humanize_relative(when, now) -> str` を追加 |
| `src/task_recorder_cui/repo.py` | Modify | `list_recent_records(conn, limit) -> list[Record]` を追加 (`ended_at IS NOT NULL` のみ、started_at DESC) |
| `src/task_recorder_cui/menu.py` | Create | メニュー本体。pure 関数 (`_active_session_line`, `_recent_records_lines`) と UI 層 (`run`, `_render_header`, `_show_main_menu`, `_dispatch`, `_pause`, `_start_flow`, `_cat_submenu`, `_HELP_TEXT`) |
| `src/task_recorder_cui/cli.py` | Modify | `args.command is None` 分岐を `menu.run()` 呼び出しに差し替え、`_not_implemented` / `_MENU_PHASE` 定数を削除 |
| `tests/test_time.py` | Modify | `humanize_relative` のテスト追加 |
| `tests/test_repo_recent.py` | Create | `list_recent_records` のテスト |
| `tests/test_menu_pure.py` | Create | pure 関数のテスト |
| `tests/test_menu_smoke.py` | Create | menu.run() スモークテスト |
| `tests/test_cli.py` | Modify | スタブ確認テストがあれば調整 (引数なしで menu.run が呼ばれる想定に合わせる) |
| `README.md` | Modify | 「インタラクティブメニュー」節を最新挙動に揃える (軽め) |

実装順は Task 1 → 2 → 3 → 4 → 5 → 6 → 7 → 8。

---

## Task 1: `humanize_relative()` を `utils/time.py` に追加

**Files:**
- Create test: `tests/test_time.py` の末尾に追加
- Modify: `src/task_recorder_cui/utils/time.py`

Spec の閾値:
- delta < 1 分 → `"たった今"`
- delta < 60 分 → `"<N>分前"`
- delta < 24 時間 → `"<N>時間前"`
- delta < 48 時間 → `"昨日"`
- それ以上 → `"<N>日前"` (`delta.days` を使用)

`when` と `now` はどちらも tz-aware な `datetime`。naive が来たら `ValueError`。

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_time.py` の末尾に追加:

```python
from datetime import UTC, datetime, timedelta

import pytest

from task_recorder_cui.utils.time import humanize_relative


@pytest.mark.parametrize(
    ("delta_seconds", "expected"),
    [
        (0, "たった今"),
        (30, "たった今"),
        (60, "1分前"),
        (59 * 60, "59分前"),
        (60 * 60, "1時間前"),
        (5 * 60 * 60, "5時間前"),
        (23 * 60 * 60, "23時間前"),
        (24 * 60 * 60, "昨日"),
        (47 * 60 * 60, "昨日"),
        (48 * 60 * 60, "2日前"),
        (3 * 24 * 60 * 60, "3日前"),
    ],
)
def test_humanize_relative_thresholds(delta_seconds: int, expected: str) -> None:
    now = datetime(2026, 4, 15, 12, 0, 0, tzinfo=UTC)
    when = now - timedelta(seconds=delta_seconds)
    assert humanize_relative(when, now) == expected


def test_humanize_relative_naive_raises() -> None:
    now = datetime(2026, 4, 15, 12, 0, 0, tzinfo=UTC)
    naive = datetime(2026, 4, 15, 11, 0, 0)
    with pytest.raises(ValueError):
        humanize_relative(naive, now)
    with pytest.raises(ValueError):
        humanize_relative(now, naive)
```

- [ ] **Step 2: テスト実行で失敗確認**

Run: `pytest tests/test_time.py -v -k humanize`
Expected: ImportError もしくは AttributeError で失敗 (`humanize_relative` が存在しない)。

- [ ] **Step 3: 最小実装を `utils/time.py` に追加**

`utils/time.py` の末尾に追加:

```python
def humanize_relative(when: datetime, now: datetime) -> str:
    """ある時刻と現在時刻の差分を「たった今/N分前/N時間前/昨日/N日前」形式で返す。

    Args:
        when: 過去の時刻 (tz付き)。
        now: 基準とする現在時刻 (tz付き)。

    Returns:
        人間向けの相対時刻文字列。

    Raises:
        ValueError: いずれかが naive な datetime の場合。

    """
    if when.tzinfo is None or now.tzinfo is None:
        raise ValueError("naive datetime is not allowed; use timezone-aware datetime")
    delta = now - when
    total_seconds = int(delta.total_seconds())
    if total_seconds < 60:
        return "たった今"
    if total_seconds < 60 * 60:
        return f"{total_seconds // 60}分前"
    if total_seconds < 24 * 60 * 60:
        return f"{total_seconds // 3600}時間前"
    if total_seconds < 48 * 60 * 60:
        return "昨日"
    return f"{delta.days}日前"
```

- [ ] **Step 4: テスト緑化を確認**

Run: `pytest tests/test_time.py -v -k humanize`
Expected: 12 件すべて PASS。

- [ ] **Step 5: 全体テスト + lint**

Run: `pytest && ruff check . && ruff format --check .`
Expected: 全テスト PASS、ruff 緑。

- [ ] **Step 6: コミット**

```bash
git add src/task_recorder_cui/utils/time.py tests/test_time.py
git commit -m "feat(utils): humanize_relative を追加 (たった今/N分前/N時間前/昨日/N日前)"
```

---

## Task 2: `repo.list_recent_records()` を追加

**Files:**
- Create test: `tests/test_repo_recent.py`
- Modify: `src/task_recorder_cui/repo.py`

仕様:
- 引数: `(conn: sqlite3.Connection, limit: int) -> list[Record]`
- `ended_at IS NOT NULL` (= 完了済み) の Record のみ
- `ORDER BY started_at DESC LIMIT <limit>`
- 0 件なら空リスト

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_repo_recent.py` を新規作成:

```python
"""repo.list_recent_records の単体テスト。"""

from datetime import UTC, datetime, timedelta

from task_recorder_cui.db import open_db
from task_recorder_cui.repo import (
    insert_record,
    list_recent_records,
    update_record_end,
)


def _add_completed(conn, key: str, started_at: datetime, ended_at: datetime, desc: str | None) -> None:
    """テスト補助: 完了済みレコードを 1 件作る。"""
    rec_id = insert_record(conn, key, desc, started_at)
    update_record_end(conn, rec_id, ended_at, int((ended_at - started_at).total_seconds() // 60))


def test_list_recent_records_returns_completed_in_desc_order(isolated_db) -> None:
    base = datetime(2026, 4, 15, 9, 0, 0, tzinfo=UTC)
    with open_db() as conn:
        with conn:
            _add_completed(conn, "dev", base, base + timedelta(minutes=30), "A")
            _add_completed(
                conn, "game", base + timedelta(hours=1), base + timedelta(hours=2), "B"
            )
            _add_completed(
                conn, "study", base + timedelta(hours=3), base + timedelta(hours=4), "C"
            )

        records = list_recent_records(conn, 10)

    assert [r.description for r in records] == ["C", "B", "A"]


def test_list_recent_records_excludes_active_sessions(isolated_db) -> None:
    base = datetime(2026, 4, 15, 9, 0, 0, tzinfo=UTC)
    with open_db() as conn:
        with conn:
            _add_completed(conn, "dev", base, base + timedelta(minutes=30), "完了済み")
            insert_record(conn, "game", "記録中", base + timedelta(hours=2))

        records = list_recent_records(conn, 10)

    assert [r.description for r in records] == ["完了済み"]


def test_list_recent_records_respects_limit(isolated_db) -> None:
    base = datetime(2026, 4, 15, 9, 0, 0, tzinfo=UTC)
    with open_db() as conn:
        with conn:
            for i in range(8):
                started = base + timedelta(hours=i)
                ended = started + timedelta(minutes=15)
                _add_completed(conn, "dev", started, ended, f"#{i}")

        records = list_recent_records(conn, 3)

    assert len(records) == 3
    assert [r.description for r in records] == ["#7", "#6", "#5"]


def test_list_recent_records_empty(isolated_db) -> None:
    with open_db() as conn:
        records = list_recent_records(conn, 5)
    assert records == []
```

- [ ] **Step 2: テスト実行で失敗確認**

Run: `pytest tests/test_repo_recent.py -v`
Expected: ImportError (`list_recent_records` が repo に無い)。

- [ ] **Step 3: `repo.py` に実装を追加**

`src/task_recorder_cui/repo.py` の末尾に追加:

```python
def list_recent_records(conn: sqlite3.Connection, limit: int) -> list[Record]:
    """完了済みレコードを新しい順に最大 limit 件返す。

    記録中セッション (`ended_at IS NULL`) は除外する。menu のヘッダ「直近」表示
    用途を想定。

    **不変条件**: 戻り値の各 Record の `ended_at` は必ず not None。

    Args:
        conn: DB接続。
        limit: 取得件数の上限 (正の整数を想定、0 以下なら空リスト)。

    Returns:
        新しい順 (started_at DESC) の Record リスト。

    """
    if limit <= 0:
        return []
    rows = conn.execute(
        "SELECT * FROM records WHERE ended_at IS NOT NULL "
        "ORDER BY started_at DESC LIMIT ?",
        (limit,),
    ).fetchall()
    return [row_to_record(r) for r in rows]
```

- [ ] **Step 4: テスト緑化を確認**

Run: `pytest tests/test_repo_recent.py -v`
Expected: 4 件すべて PASS。

- [ ] **Step 5: 全体テスト + lint**

Run: `pytest && ruff check . && ruff format --check .`
Expected: 全 PASS、ruff 緑。

- [ ] **Step 6: コミット**

```bash
git add src/task_recorder_cui/repo.py tests/test_repo_recent.py
git commit -m "feat(repo): list_recent_records を追加 (ended_at NOT NULL のみ DESC)"
```

---

## Task 3: menu.py の pure 関数 (`_active_session_line`, `_recent_records_lines`) を追加

**Files:**
- Create: `src/task_recorder_cui/menu.py` (pure 関数のみ、UI 層は次タスク)
- Create test: `tests/test_menu_pure.py`

仕様:

`_active_session_line(now: datetime) -> str`:
- DB の `find_active_record` を呼ぶ
- 記録中があれば `"現在: [<display_name>] <description> (<経過>経過)"` (description が None なら `"(記録なし)"` 部分は省略しない: `"現在: [<display_name>]  (<経過>経過)"` のように 2 連続スペース)
- 経過時間は `format_duration(int((now - started_at).total_seconds() // 60))`
- カテゴリの display_name は `repo.find_category(conn, key)` で引く (見つからない場合は `category_key` をそのまま使用)
- 記録中なし → `"現在: 記録なし"`

`_recent_records_lines(now: datetime, limit: int = 5) -> list[str]`:
- `repo.list_recent_records(conn, limit)` を呼ぶ
- 各レコードを `"  [<display_name>] <description>  <duration>  <relative>"` 形式 (頭 2 スペースインデント)
  - description が None → 空文字
  - duration は `format_duration(record.duration_minutes)`
  - relative は `humanize_relative(record.started_at, now)`
- カテゴリ display_name はキャッシュ用の dict (1 回だけ全カテゴリ取得) で解決
- 0 件なら空リスト

**重要**: pure 関数は副作用を持たない。DB 読み取りは行うが print しない。

- [ ] **Step 1: 失敗するテストを書く**

`tests/test_menu_pure.py` を新規作成:

```python
"""menu.py の pure 関数の単体テスト (副作用なし、DB 読み取りのみ)。"""

from datetime import UTC, datetime, timedelta

from task_recorder_cui.db import open_db
from task_recorder_cui.menu import _active_session_line, _recent_records_lines
from task_recorder_cui.repo import insert_record, update_record_end


def test_active_session_line_when_recording(isolated_db) -> None:
    started = datetime(2026, 4, 15, 12, 0, 0, tzinfo=UTC)
    now = started + timedelta(minutes=32)
    with open_db() as conn:
        with conn:
            insert_record(conn, "dev", "ObatLog実装", started)

    line = _active_session_line(now)
    assert "現在:" in line
    assert "[開発]" in line  # display_name が解決される
    assert "ObatLog実装" in line
    assert "32m" in line
    assert "経過" in line


def test_active_session_line_when_no_description(isolated_db) -> None:
    started = datetime(2026, 4, 15, 12, 0, 0, tzinfo=UTC)
    now = started + timedelta(minutes=10)
    with open_db() as conn:
        with conn:
            insert_record(conn, "game", None, started)

    line = _active_session_line(now)
    assert "[ゲーム]" in line
    assert "10m" in line


def test_active_session_line_when_idle(isolated_db) -> None:
    now = datetime(2026, 4, 15, 12, 0, 0, tzinfo=UTC)
    line = _active_session_line(now)
    assert line == "現在: 記録なし"


def test_recent_records_lines_empty(isolated_db) -> None:
    now = datetime(2026, 4, 15, 12, 0, 0, tzinfo=UTC)
    lines = _recent_records_lines(now, limit=5)
    assert lines == []


def test_recent_records_lines_format_and_order(isolated_db) -> None:
    base = datetime(2026, 4, 15, 8, 0, 0, tzinfo=UTC)
    now = datetime(2026, 4, 15, 13, 0, 0, tzinfo=UTC)
    with open_db() as conn:
        with conn:
            for i, key in enumerate(["dev", "game", "study"]):
                started = base + timedelta(hours=i)
                ended = started + timedelta(minutes=30 + i * 15)
                rec_id = insert_record(conn, key, f"item-{i}", started)
                update_record_end(
                    conn, rec_id, ended, int((ended - started).total_seconds() // 60)
                )

    lines = _recent_records_lines(now, limit=5)
    assert len(lines) == 3
    # 新しい順
    assert "item-2" in lines[0]
    assert "item-1" in lines[1]
    assert "item-0" in lines[2]
    # display_name が解決される (dev → 開発、game → ゲーム、study → 学習)
    assert "[学習]" in lines[0]
    assert "[ゲーム]" in lines[1]
    assert "[開発]" in lines[2]
    # 各行は 2 スペースインデント
    for line in lines:
        assert line.startswith("  ")


def test_recent_records_lines_respects_limit(isolated_db) -> None:
    base = datetime(2026, 4, 15, 8, 0, 0, tzinfo=UTC)
    now = datetime(2026, 4, 15, 14, 0, 0, tzinfo=UTC)
    with open_db() as conn:
        with conn:
            for i in range(7):
                started = base + timedelta(hours=i * 0.5)
                ended = started + timedelta(minutes=10)
                rec_id = insert_record(conn, "dev", f"#{i}", started)
                update_record_end(
                    conn, rec_id, ended, int((ended - started).total_seconds() // 60)
                )

    lines = _recent_records_lines(now, limit=3)
    assert len(lines) == 3
```

- [ ] **Step 2: テスト実行で失敗確認**

Run: `pytest tests/test_menu_pure.py -v`
Expected: ImportError (`task_recorder_cui.menu` が無い)。

- [ ] **Step 3: `menu.py` に pure 関数だけ実装**

`src/task_recorder_cui/menu.py` を新規作成 (pure 関数のみ):

```python
"""tsk のインタラクティブメニュー実装。

`tsk` をサブコマンドなしで起動した時に呼ばれる。pure 関数 (DB 読み取りのみ、
副作用なし) と UI 層 (questionary + print) を同一ファイルに同居させ、テストは
pure 関数中心に当てる方針。
"""

from datetime import datetime

from task_recorder_cui.db import open_db
from task_recorder_cui.repo import find_active_record, find_category, list_recent_records
from task_recorder_cui.utils.time import format_duration, humanize_relative, now_utc


def _active_session_line(now: datetime) -> str:
    """記録中セッションの 1 行表示を返す (副作用なし)。

    Args:
        now: 経過時間計算の基準時刻 (tz付き)。

    Returns:
        "現在: [<display>] <desc> (<経過>経過)" もしくは "現在: 記録なし"。

    """
    with open_db() as conn:
        active = find_active_record(conn)
        if active is None:
            return "現在: 記録なし"
        category = find_category(conn, active.category_key)
        display = category.display_name if category is not None else active.category_key

    elapsed_minutes = max(0, int((now - active.started_at).total_seconds() // 60))
    elapsed = format_duration(elapsed_minutes)
    desc = active.description or ""
    return f"現在: [{display}] {desc} ({elapsed}経過)"


def _recent_records_lines(now: datetime, limit: int = 5) -> list[str]:
    """直近の完了済みレコードを 1 行ずつ整形して返す (副作用なし)。

    Args:
        now: 相対時刻計算の基準時刻 (tz付き)。
        limit: 取得件数。

    Returns:
        各行は "  [<display>] <desc>  <duration>  <relative>" 形式。

    """
    with open_db() as conn:
        records = list_recent_records(conn, limit)
        if not records:
            return []
        # display_name 解決用に全カテゴリを 1 回だけ引く
        from task_recorder_cui.repo import list_all_categories

        cats = {c.key: c.display_name for c in list_all_categories(conn)}

    lines: list[str] = []
    for r in records:
        display = cats.get(r.category_key, r.category_key)
        desc = r.description or ""
        duration = format_duration(r.duration_minutes or 0)
        relative = humanize_relative(r.started_at, now)
        lines.append(f"  [{display}] {desc}  {duration}  {relative}")
    return lines
```

- [ ] **Step 4: テスト緑化を確認**

Run: `pytest tests/test_menu_pure.py -v`
Expected: 6 件すべて PASS。

- [ ] **Step 5: 全体テスト + lint**

Run: `pytest && ruff check . && ruff format --check .`
Expected: 全 PASS、ruff 緑。format で警告出たら `ruff format .` 後に再確認。

- [ ] **Step 6: コミット**

```bash
git add src/task_recorder_cui/menu.py tests/test_menu_pure.py
git commit -m "feat(menu): pure 関数 (_active_session_line / _recent_records_lines) を追加"
```

---

## Task 4: menu.py UI 層 (メインループ + ヘルプ + 単純ディスパッチ) と cli.py の差し替え

**Files:**
- Modify: `src/task_recorder_cui/menu.py`
- Modify: `src/task_recorder_cui/cli.py`

含めるもの:
- `_HELP_TEXT` 定数 (CLI コマンド一覧チートシート + 「CLI 仕様変更時はここも更新すること」コメント)
- `_render_header(now)` (`print_line` で active_session_line + 直近)
- `_pause()` (input ベース、KeyboardInterrupt と EOFError を握りつぶす)
- `_show_main_menu(recording: bool) -> str | None` (questionary.select、停止は `not recording` で disabled)
- `_dispatch(choice)` (start/stop/today/week/month/help/cat 分岐、start と cat は次タスクで本実装、ここではスタブで `print_line` だけ)
- `run() -> int` (メインループ、Ctrl+C 吸収、quit / None で 0 終了)
- `cli.py` の `args.command is None` 分岐を `menu.run()` に差し替え、`_not_implemented` / `_MENU_PHASE` を削除

start / cat はこのタスクではスタブ実装 (`print_line("(start フローは Task 5 で実装)")` のような) にしておき、Task 5 で本実装する。これにより Task 4 単独で push できる。

- [ ] **Step 1: `menu.py` に UI 層を追記**

`src/task_recorder_cui/menu.py` の末尾に追加 (既存 import の下に questionary 系を足す):

import 部を以下に置換:

```python
from datetime import datetime

import questionary

from task_recorder_cui.commands import month as month_cmd
from task_recorder_cui.commands import stop as stop_cmd
from task_recorder_cui.commands import today as today_cmd
from task_recorder_cui.commands import week as week_cmd
from task_recorder_cui.db import open_db
from task_recorder_cui.io import print_line
from task_recorder_cui.repo import (
    find_active_record,
    find_category,
    list_all_categories,
    list_recent_records,
)
from task_recorder_cui.utils.time import format_duration, humanize_relative, now_utc
```

(注: pure 関数内で行っていた `from task_recorder_cui.repo import list_all_categories` のローカル import は削除し、上の trunk import に統一する)

ファイル末尾に追加:

```python
# NOTE: CLI の仕様変更時はここも更新すること (cli.py / argparse が真実)
_HELP_TEXT = """tsk コマンド一覧

記録:
  tsk start <cat> ["<desc>"]             新しいセッションを開始
  tsk stop                                記録中のセッションを終了
  tsk add   <cat> <分> ["<desc>"]        事後に手動で追加
  tsk now                                 記録中セッションを表示

集計:
  tsk today                               今日の一覧
  tsk week  [--calendar]                  直近7日 / 今週
  tsk month [--calendar]                  直近30日 / 今月
  tsk range --from YYYY-MM-DD --to YYYY-MM-DD   任意期間
  tsk all                                 全累計

カテゴリ:
  tsk cat list [--active|--archived]
  tsk cat add     <key> "<表示名>"
  tsk cat remove  <key>
  tsk cat restore <key>
  tsk cat rename  <key> "<新表示名>"

その他:
  tsk --version / --help
  tsk (引数なし)                          このメニューを起動
"""


def _render_header(now: datetime) -> None:
    """ヘッダ (タイトル + 現在のセッション + 直近) を描画する。"""
    print_line()
    print_line("tsk - task recorder")
    print_line()
    print_line(_active_session_line(now))
    recent = _recent_records_lines(now)
    if recent:
        print_line()
        print_line("直近:")
        for line in recent:
            print_line(line)
    print_line()


def _pause() -> None:
    """Enter で戻る。Ctrl+C / EOF は握りつぶしてループに戻す。"""
    try:
        input("[Enter で戻る]")
    except (KeyboardInterrupt, EOFError):
        pass


def _show_main_menu(*, recording: bool) -> str | None:
    """メインメニューを表示し選択値 (value 文字列) を返す。Ctrl+C / ESC で None。"""
    stop_disabled: str | bool = (
        False if recording else "(記録中のセッションがありません)"
    )
    return questionary.select(
        "操作を選んでください",
        choices=[
            questionary.Choice("開始", value="start"),
            questionary.Choice("停止", value="stop", disabled=stop_disabled),
            questionary.Choice("今日の一覧", value="today"),
            questionary.Choice("週集計", value="week"),
            questionary.Choice("月集計", value="month"),
            questionary.Choice("カテゴリ管理", value="cat"),
            questionary.Choice("ヘルプ (CLI コマンド一覧)", value="help"),
            questionary.Choice("終了", value="quit"),
        ],
    ).ask()


def _show_help() -> None:
    """ヘルプテキストを表示する。"""
    print_line(_HELP_TEXT)


def _start_flow() -> None:
    """[Task 5 で本実装] カテゴリ選択 → description 入力 → start 実行。"""
    print_line("(start フローは Task 5 で実装)")


def _cat_submenu() -> None:
    """[Task 5 で本実装] カテゴリ管理サブメニュー。"""
    print_line("(カテゴリ管理サブメニューは Task 5 で実装)")


def _dispatch(choice: str) -> None:
    """選択値に応じて各アクションを呼ぶ。戻り値は捨てる。"""
    if choice == "start":
        _start_flow()
        return
    if choice == "stop":
        stop_cmd.run()
        return
    if choice == "today":
        today_cmd.run()
        return
    if choice == "week":
        week_cmd.run(calendar=False)
        return
    if choice == "month":
        month_cmd.run(calendar=False)
        return
    if choice == "cat":
        _cat_submenu()
        return
    if choice == "help":
        _show_help()
        return
    # 未知の値は安全に無視
    print_line(f"(未知の選択肢: {choice})")


def run() -> int:
    """メニューのメインループ。常に 0 を返す。

    内部で呼ぶ commands.*.run() の戻り値は意図的に捨てる: メニューはラッパー層で
    あり、終了コードでの成否伝搬は CLI 単発呼び出しの責務とする。

    Returns:
        常に 0。

    """
    while True:
        now = now_utc()
        _render_header(now)
        with open_db() as conn:
            recording = find_active_record(conn) is not None
        choice = _show_main_menu(recording=recording)
        if choice is None or choice == "quit":
            return 0
        try:
            _dispatch(choice)
        except KeyboardInterrupt:
            print_line("(中断しました)")
            continue
        _pause()
```

- [ ] **Step 2: pure 関数のローカル import を整理 (重複削除)**

`menu.py` の `_recent_records_lines` 内にある:

```python
        from task_recorder_cui.repo import list_all_categories
```

の 1 行を削除する (Step 1 で trunk の import に追加済み)。同名関数が trunk import に来ているので、関数内で改めて import する必要が無い。

- [ ] **Step 3: `cli.py` の `args.command is None` 分岐を差し替え**

`src/task_recorder_cui/cli.py` の以下の部分を変更:

旧 (削除対象):

```python
from task_recorder_cui.io import print_line

_MENU_PHASE = "Phase 6"


def _not_implemented(feature: str, phase: str) -> int:
    """未実装コマンド用のスタブ応答。
    ...
    """
    print_line(f"({feature} は {phase} で実装予定)")
    return 0
```

→ 上のスタブ関数定義と `_MENU_PHASE` 定数の 2 ブロックを削除する。`from task_recorder_cui.io import print_line` の行は他で使われていないなら削除する。`grep print_line src/task_recorder_cui/cli.py` で残存使用が無いことを事前に確認。

旧 (差し替え対象):

```python
    if args.command is None:
        return _not_implemented("インタラクティブメニュー", _MENU_PHASE)
```

新:

```python
    if args.command is None:
        from task_recorder_cui.menu import run as menu_run

        return menu_run()
```

(menu のローカル import: cli が起動時に毎回 questionary を読み込まないようにする = サブコマンド指定時の起動を軽くする)

ファイル冒頭の docstring の「インタラクティブメニューは Phase 6 まで未実装スタブで応答する。」の文を「インタラクティブメニューは `menu.run()` に dispatch する。」に書き換える。

- [ ] **Step 4: pytest と ruff で全体グリーン確認**

Run: `pytest && ruff check . && ruff format --check .`
Expected: 全 PASS、ruff 緑。`tests/test_cli.py` で「Phase 6 で実装予定」のような文字列を含むテストがあれば、その箇所はメニューが呼ばれて即終了 (例: questionary を monkeypatch して quit) する形に修正する必要がある。下の Step 5 で対処。

- [ ] **Step 5: 既存 `tests/test_cli.py` の調整**

`grep -n "Phase 6\|未実装\|_not_implemented" tests/test_cli.py` で関連テストを確認。該当テストがあれば以下のいずれかで対処:

- (a) 該当テストを削除し、代わりに「`tsk` 引数なしで `menu.run` が呼ばれる」テストを 1 本追加する:

```python
def test_main_no_command_calls_menu(monkeypatch):
    called = {"n": 0}

    def fake_run() -> int:
        called["n"] += 1
        return 0

    monkeypatch.setattr("task_recorder_cui.menu.run", fake_run)
    from task_recorder_cui.cli import main

    rc = main([])
    assert rc == 0
    assert called["n"] == 1
```

- (b) 該当テストが存在しなければ何もしない。`grep` でヒット 0 行ならスキップ。

- [ ] **Step 6: 再度全体テストと lint**

Run: `pytest && ruff check . && ruff format --check .`
Expected: 全 PASS、ruff 緑。

- [ ] **Step 7: コミット**

```bash
git add src/task_recorder_cui/menu.py src/task_recorder_cui/cli.py tests/test_cli.py
git commit -m "feat(menu): UI 層スケルトン (メインループ/ヘルプ/ヘッダ/_pause) と cli 配線"
```

---

## Task 5: `_start_flow()` と `_cat_submenu()` の本実装

**Files:**
- Modify: `src/task_recorder_cui/menu.py`

仕様 (設計書「開始フローの詳細」「カテゴリ管理サブメニュー」より):

`_start_flow()`:
1. active カテゴリを取得 (0 件なら案内して return)
2. `questionary.select("カテゴリを選んでください", choices=...)` で選択 (`title=f"{display_name} ({key})"`, `value=key`)
3. キャンセル (`None`) なら return
4. `questionary.text("何をしましたか (任意、空欄可)", default="")` で description (空文字 → None)
5. `start_cmd.run(key, desc_or_none)` を呼ぶ (戻り値は捨てる)

`_cat_submenu()`:
- ループ構造 (`while True`)、選択肢: 一覧表示 / 追加 / アーカイブ / アーカイブから復帰 / 表示名を変更 / ← 戻る
- `back` または `None` でループ脱出
- 各アクション後は `_pause()` → 再描画
- アクション詳細:
  - **list**: `cat_cmd.list_categories(active_only=False, archived_only=False)`
  - **add**: `questionary.text("新しいカテゴリキー (ASCII 英小文字・数字・_)")` → `questionary.text("表示名")` → `cat_cmd.add_category(key, display_name)`。どちらかが `None` (キャンセル) なら戻る
  - **remove**: active カテゴリ select (0 件なら警告) → `questionary.confirm(f"{display_name} ({key}) をアーカイブしますか？", default=False)` → True なら `cat_cmd.remove_category(key)`
  - **restore**: archived カテゴリ select (0 件なら警告) → `cat_cmd.restore_category(key)`
  - **rename**: active カテゴリ select → `questionary.text("新しい表示名", default=現在名)` → `cat_cmd.rename_category(key, new_name)`

- [ ] **Step 1: `menu.py` に start / cat 用の commands import を追加**

`menu.py` の trunk import に以下を追加:

```python
from task_recorder_cui.commands import cat as cat_cmd
from task_recorder_cui.commands import start as start_cmd
```

(既存の `from task_recorder_cui.commands import month as month_cmd` などの並びの直前または直後)

- [ ] **Step 2: `_start_flow()` を本実装に差し替え**

`menu.py` の `_start_flow()` のスタブ本体を以下に置き換える:

```python
def _start_flow() -> None:
    """カテゴリ選択 → description 入力 → start 実行。

    active カテゴリが 0 件なら案内のみ。キャンセル (Ctrl+C/ESC) で安全に戻る。
    """
    with open_db() as conn:
        actives = list_all_categories(conn, active_only=True)

    if not actives:
        print_line(
            "有効なカテゴリがありません。先に『カテゴリ管理 → 追加』してください。"
        )
        return

    key = questionary.select(
        "カテゴリを選んでください",
        choices=[
            questionary.Choice(title=f"{c.display_name} ({c.key})", value=c.key)
            for c in actives
        ],
    ).ask()
    if key is None:
        return

    desc = questionary.text("何をしましたか (任意、空欄可)", default="").ask()
    if desc is None:
        return
    desc_or_none: str | None = desc.strip() or None

    start_cmd.run(key, desc_or_none)
```

- [ ] **Step 3: `_cat_submenu()` を本実装に差し替え**

`menu.py` の `_cat_submenu()` のスタブ本体を以下に置き換える:

```python
def _cat_submenu() -> None:
    """カテゴリ管理サブメニュー (list/add/remove/restore/rename/back のループ)。"""
    while True:
        action = questionary.select(
            "カテゴリ管理",
            choices=[
                questionary.Choice("一覧表示", value="list"),
                questionary.Choice("追加", value="add"),
                questionary.Choice("アーカイブ", value="remove"),
                questionary.Choice("アーカイブから復帰", value="restore"),
                questionary.Choice("表示名を変更", value="rename"),
                questionary.Choice("← 戻る", value="back"),
            ],
        ).ask()
        if action is None or action == "back":
            return

        if action == "list":
            cat_cmd.list_categories(active_only=False, archived_only=False)
        elif action == "add":
            _cat_add()
        elif action == "remove":
            _cat_remove()
        elif action == "restore":
            _cat_restore()
        elif action == "rename":
            _cat_rename()

        _pause()


def _cat_add() -> None:
    """カテゴリ追加 (key / display_name を順に入力)。"""
    key = questionary.text(
        "新しいカテゴリキー (ASCII 英小文字・数字・_)"
    ).ask()
    if key is None or not key.strip():
        return
    display_name = questionary.text("表示名").ask()
    if display_name is None or not display_name.strip():
        return
    cat_cmd.add_category(key.strip(), display_name.strip())


def _cat_remove() -> None:
    """カテゴリのアーカイブ (active から選択 → confirm)。"""
    with open_db() as conn:
        actives = list_all_categories(conn, active_only=True)
    if not actives:
        print_line("active なカテゴリがありません")
        return
    key = questionary.select(
        "アーカイブするカテゴリを選んでください",
        choices=[
            questionary.Choice(title=f"{c.display_name} ({c.key})", value=c.key)
            for c in actives
        ],
    ).ask()
    if key is None:
        return
    target = next(c for c in actives if c.key == key)
    confirmed = questionary.confirm(
        f"{target.display_name} ({target.key}) をアーカイブしますか？",
        default=False,
    ).ask()
    if not confirmed:
        return
    cat_cmd.remove_category(key)


def _cat_restore() -> None:
    """archived カテゴリの復帰。"""
    with open_db() as conn:
        archived = list_all_categories(conn, archived_only=True)
    if not archived:
        print_line("archived なカテゴリがありません")
        return
    key = questionary.select(
        "復帰させるカテゴリを選んでください",
        choices=[
            questionary.Choice(title=f"{c.display_name} ({c.key})", value=c.key)
            for c in archived
        ],
    ).ask()
    if key is None:
        return
    cat_cmd.restore_category(key)


def _cat_rename() -> None:
    """display_name の変更 (active から選択 → 新名入力)。"""
    with open_db() as conn:
        actives = list_all_categories(conn, active_only=True)
    if not actives:
        print_line("active なカテゴリがありません")
        return
    key = questionary.select(
        "表示名を変更するカテゴリを選んでください",
        choices=[
            questionary.Choice(title=f"{c.display_name} ({c.key})", value=c.key)
            for c in actives
        ],
    ).ask()
    if key is None:
        return
    target = next(c for c in actives if c.key == key)
    new_name = questionary.text("新しい表示名", default=target.display_name).ask()
    if new_name is None or not new_name.strip():
        return
    cat_cmd.rename_category(key, new_name.strip())
```

- [ ] **Step 4: 全体テスト + lint**

Run: `pytest && ruff check . && ruff format --check .`
Expected: 全 PASS、ruff 緑。

- [ ] **Step 5: ローカル動作確認 (手動)**

Run: `tsk` (引数なし)
Expected: メニューが表示され、矢印キーで選択可能。「ヘルプ」「今日」「カテゴリ管理 → 一覧表示」が動く。記録中でない時に「停止」がグレーアウト。最後に「終了」で抜けてプロンプトに戻る。

このステップは目視確認のみ。問題なければ次へ。

- [ ] **Step 6: コミット**

```bash
git add src/task_recorder_cui/menu.py
git commit -m "feat(menu): start フローと カテゴリ管理サブメニュー (5 アクション + confirm) を実装"
```

---

## Task 6: スモークテスト追加 + README 更新

**Files:**
- Create: `tests/test_menu_smoke.py`
- Modify: `README.md`

スモークテストは `_show_main_menu` を monkeypatch で `"quit"` を返すよう差し替え、`menu.run()` の戻り値が `0` であること + DB に何も書き込まれていないことを確認する。

README は既に「インタラクティブメニュー」節があるので、「Phase 6 まで未実装」のような表記が残っていれば外し、ヘルプ項目とカテゴリ管理サブメニューに言及する。

- [ ] **Step 1: スモークテストを作成**

`tests/test_menu_smoke.py` を新規作成:

```python
"""menu.run() のスモークテスト (UI を最小モックして 0 終了することだけ確認)。"""

from task_recorder_cui.db import open_db


def test_menu_run_quit_returns_zero(isolated_db, monkeypatch) -> None:
    """quit を選んだら戻り値 0、DB は空のまま。"""
    monkeypatch.setattr(
        "task_recorder_cui.menu._show_main_menu",
        lambda *, recording: "quit",
    )

    from task_recorder_cui.menu import run

    rc = run()
    assert rc == 0

    with open_db() as conn:
        rec_count = conn.execute("SELECT COUNT(*) FROM records").fetchone()[0]
    assert rec_count == 0


def test_menu_run_returns_zero_on_none(isolated_db, monkeypatch) -> None:
    """ESC / Ctrl+C で None が返っても 0 終了。"""
    monkeypatch.setattr(
        "task_recorder_cui.menu._show_main_menu",
        lambda *, recording: None,
    )

    from task_recorder_cui.menu import run

    assert run() == 0
```

- [ ] **Step 2: スモークテスト実行で緑化確認**

Run: `pytest tests/test_menu_smoke.py -v`
Expected: 2 件 PASS。

- [ ] **Step 3: README の該当節を更新**

`README.md` の以下の節を修正:

旧 (該当箇所、既存):

```markdown
### インタラクティブメニュー

サブコマンドなしで起動すると、階層メニューから操作できます。

```bash
tsk
```

(以下、Box 描画モックアップ)
```

そのまま残してよいが、モックアップに「ヘルプ」項目を追加する。具体的には Box 内の項目リスト 5) 月集計 と 6) カテゴリ管理 の間に追加…ではなく、現状の README は項目を 1)〜q) で書いてあるので、以下を整える:

該当のモックアップ (`### インタラクティブメニュー` 配下の Box 部分) を以下で置き換える:

````markdown
```
┌─────────────────────────────────────┐
│ tsk - task recorder                 │
├─────────────────────────────────────┤
│ 現在: [開発] ObatLog実装 (32m経過)  │
├─────────────────────────────────────┤
│ ? 操作を選んでください              │
│   開始                              │
│   停止                              │
│   今日の一覧                        │
│   週集計                            │
│   月集計                            │
│   カテゴリ管理                      │
│   ヘルプ (CLI コマンド一覧)         │
│   終了                              │
└─────────────────────────────────────┘
```

矢印キー + Enter で選択します。Ctrl+C / ESC でいつでも安全に中断できます (記録中のセッションは保持されます)。
````

- [ ] **Step 4: 全体テスト + lint**

Run: `pytest --cov=task_recorder_cui --cov-report=term && ruff check . && ruff format --check .`
Expected: 全 PASS、coverage が既存水準 (90%+) を維持、ruff 緑。

- [ ] **Step 5: コミット**

```bash
git add tests/test_menu_smoke.py README.md
git commit -m "test(menu): スモークテスト追加 と README のメニュー節を最新化"
```

---

## Task 7: push + PR + Actions 完走確認

**Files:** なし (git / gh 操作のみ)

- [ ] **Step 1: 現在のブランチを確認**

Run: `git rev-parse --abbrev-ref HEAD`
Expected: `feature/menu`。

- [ ] **Step 2: 最終ローカル確認 (ダブルチェック)**

Run: `ruff check . && ruff format --check . && pytest --cov=task_recorder_cui --cov-report=term`
Expected: 全グリーン、coverage 90%+ を維持。

- [ ] **Step 3: `feature/menu` を origin に push**

Run: `git push -u origin feature/menu`
Expected: `new branch` で push 成功。

- [ ] **Step 4: dev 向けに PR を作成**

Run:

```bash
gh pr create --base dev --head feature/menu \
  --title "feat: インタラクティブメニュー (Phase 6) を実装 (closes #13)" \
  --body "$(cat <<'EOF'
## Summary
- `tsk` (引数なし) で questionary ベースの階層メニューを起動
- メインメニュー: 開始 / 停止 (条件付き disable) / 今日 / 週 / 月 / カテゴリ管理 / ヘルプ / 終了
- カテゴリ管理サブメニュー: list / add / remove (confirm 付き) / restore / rename / 戻る
- ヘッダに現在のセッションと直近 5 件を表示
- 静的ヘルプテキスト (CLI コマンド一覧) を Help から閲覧可能
- pure 関数 (active_session_line / recent_records_lines / humanize_relative) は単体テスト、UI はスモークテスト 1 本

## 設計
- 設計書: `docs/superpowers/specs/2026-04-15-menu-design.md`
- 実装計画: `docs/superpowers/plans/2026-04-15-menu.md`
- Issue: #13

## 戻り値ポリシー
- `menu.run()` は常に 0 を返す
- 内部の `commands.*.run()` の戻り値は意図的に捨てる (メニューはラッパー層)

## Test plan
- [ ] 全マトリクス (3.11 / 3.12 / 3.13) の Actions グリーン
- [ ] Codecov 数値が既存水準 (90%+) を維持
- [ ] ローカルで `tsk` 起動 → ヘルプ・カテゴリ管理・終了が一通り動く
EOF
)"
```

Expected: PR URL が表示される。

- [ ] **Step 5: Actions の完走を待つ**

Run: `gh pr checks --watch`
Expected: 全マトリクスと Codecov upload が pass で終わる。

- [ ] **Step 6: PR URL をユーザに共有してマージ待ち**

Run: `gh pr view --json url -q .url`
Expected: PR URL が表示される。マージはユーザが実施。

---

## 成功基準 (仕上げチェック)

- [ ] PR の Actions が全マトリクスでグリーン
- [ ] Codecov カバレッジが 90%+ を維持
- [ ] ローカルで `tsk` 起動 → ヘッダ + メニュー → 矢印で「開始」「ヘルプ」「カテゴリ管理 → 一覧」が動く
- [ ] 「停止」が記録中でない時にグレーアウト表示
- [ ] Ctrl+C でメニュー・プロンプト中にクラッシュしない
- [ ] ユーザが PR をマージ
